// Maximum number of photos that can be marked "featured" within a single
// album — keeps the admin's spotlight selection intentional rather than
// starring an entire album. This is unrelated to the public Gallery page's
// own MAX_FEATURED_PHOTOS, which caps how many featured photos are shown
// across *all* albums in the homepage-style spotlight slideshow.
export const MAX_FEATURED_PER_ALBUM = 10;
